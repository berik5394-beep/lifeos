# LifeOS — Аудит и план устранения (2026-06-01)

> **ЕДИНСТВЕННЫЙ актуальный аудит проекта.** Все прежние AUDIT-*.md и LIFEOS-STATUS.md удалены — не доверять им. Этот файл = источник правды + живой трекер. Чиним **по порядку: сначала косяки кода → в конце App Store.**

## Как работаем (протокол)
- Идём строго по фазам сверху вниз. Один пункт = один аккуратный заход (понять → фикс → тест/tsc → локальный коммит).
- **Push/deploy/flag — только по явному «пуш и деплой» от Berik.** Коммиты локальные, пока не сказано пушить.
- Прод-БД НЕ трогаем напрямую; миграции — только локальный docker, на прод через Dockerfile при деплое.
- Принцип: **если фича битая и не ядро (память+проактивность) — удаляем, а не чиним** (YAGNI). Ядро бережём.
- Отмечаем `[x]` по мере закрытия. Verify по факту (БД/тест), не по нарративу.

Статус-метки: ⬜ не начато · 🔄 в работе · ✅ сделано (локально) · 🚀 в проде.

---

## ФАЗА 0 — Гигиена и чистка (быстро, первым делом)

- [x] **0.1** Удалить старые аудиты: `AUDIT-2026-05.md`, `AUDIT-ARCHITECTURE.md`, `AUDIT-CODE-REVIEW.md`, `AUDIT-FRONTEND.md`, `AUDIT-SECURITY.md`, `AUDIT-SUMMARY.md`. ✅
- [~] **0.2** 🔴 Секреты. **Файл `LIFEOS-STATUS.md` удалён** (commit `302faa8`) — содержал live Groq-ключ `gsk_` + тестовый пароль. Скан всех tracked-файлов: больше реальных ключей нет (`.env.example` = плейсхолдеры `xxx`, JARVIS_SPEC/ping-models чисты). **⚠️ ОСТАЁТСЯ (только Berik, в дашбордах):** (1) **ротировать Groq-ключ** на console.groq.com — он НАВСЕГДА в git-истории, удаление файла его не стирает; после ротации обновить `GROQ_API_KEY` в Railway env + локальном `.env`. (2) **ротировать Railway API-токен** (светился в чате).
- [x] **0.3** ✅ **Фича калорий/питания удалена целиком** (commit `90365e7`, локальный): NutritionLog + relation + дроп-миграция; food-роуты vision.ts; delete-account; mobile nutrition.tsx/screens/nav + swipe-home плитка; camera/photo строки переписаны, NSHealth* убраны. Сервер tsc+1809 тестов зелёные, мобайл tsc чист, миграция идемпотентна (local docker). Прод-БД не трогали — DROP применится через migrate deploy при деплое. (Активные калории в health-connect — отдельный фитнес-стаб, оставлен.) ~~Детали:~~
  - Сервер: модель `NutritionLog` (schema.prisma:631) + relation `User.nutritionLogs` (:56) + дроп-миграция; роуты еды в `routes/vision.ts` (анализ фото→калории, save, list ~стр.147–290); ссылка в `routes/auth.ts` delete-account; упоминания в `planner-service.ts`, `interest-service.ts`.
  - Мобайл: `app/nutrition.tsx` (498), `screens/nutrition.ts`, навигация (`navigation/index.tsx:43,402`, `navigation/types.ts:14`), калорийные куски в `services/health-sync.ts` / `health-connect.ts`, ссылки в `task-capture.tsx`, `swipe-home.tsx`, `settings/integrations.tsx`.
  - Заодно: убрать неиспользуемые `NSHealth*` usage-strings из `app.json` (HealthKit-модуля нет, только шагомер).

---

## ФАЗА 1 — Прод-надёжность (сервер, уже живой) — ГЛАВНОЕ

- [x] **1.1** ✅ **C1 закрыт** (commit `d408123`). `parseIntent` Claude-вызов обёрнут в try/catch; на сбой логирует + возвращает `{action:'unknown'}` → orchestrator уводит в agent-путь с его лестницей деградации (мягкий ответ вместо 500). Вложенный JSON-парс сохранён. +2 структурных теста; сюита 1811 зелёная, tsc чист. (Claude-путь не тестим поведенчески — конвенция файла.)
- [x] **1.2** ✅ **C2 закрыт** (commit `f01ede4`). `lib/anthropic.ts` `createAnthropic()` = 60с timeout + явный maxRetries; все 25 call-sites мигрированы; `lib/anthropic-discipline.test.ts` structural-lint запрещает `new Anthropic(` вне фабрики (CI красный до прода). Verified: 0 сырых конструкторов, tsc чист, сюита 1812 зелёная.
- [s] **1.3** ⏭️ **ПРОПУЩЕНО (Berik 2026-06-01):** Telegram = тестовая ветка; в релизе он станет просто **оповещением «открой приложение»**, а не полноценным AI-каналом. Харденить AI-rate-limit на нём смысла нет. Риск неконтролируемых трат через бота закрывается тем, что бот перестанет звать Claude (станет push-only). ⚠️ Если Telegram останется открытым публичным AI-каналом — вернуться к этому.
- [x] **1.4** ✅ **Закрыт** (commit `46e5324`). `captureInBackground` теперь fire-and-forget (void + `.catch`) на `jarvis-orchestrator.ts:1020` — ответ не ждёт +1 Sonnet-вызов. +2 структурных теста.
- [x] **1.5** ✅ **Закрыт** (commit `46e5324`). `index.ts`: `process.on('unhandledRejection')` логирует (процесс живёт), `process.on('uncaughtException')` логирует + graceful shutdown (Railway рестартит). +3 теста. tsc чист, сюита 1817.

---

## ФАЗА 2 — Инфра и безопасность масштабирования

- [x] **2.1** ✅ **Кроны-двойники закрыты** (commit `32207c0`). `CronJobRun.lockKey String? @unique` (job:user:период-бакет) + `withCronLock` теперь claim-before-run: insert claim ДО fn → конкуренты ловят P2002 и пропускают; на throw claim удаляется (retry-on-failure сохранён). Идемпотентная миграция `20260601140000_cron_lock_key` (local docker, прод через migrate deploy). tsc чист, сюита 1818.
- [ ] **2.1b** 🟡 **In-memory OAuth-state** (`google-calendar.ts:93` oauthStates Map) → Postgres (как PendingAction). Ломается только при рестарте посреди OAuth-флоу (10-мин окно) или multi-instance; на 1 инстансе низкий риск, деградирует мягко («invalid state», юзер ретраит). Отдельный больший change (таблица + миграция + рефактор createAuthUrl/consumeAuthState). Решить: делать сейчас или отложить.
- [ ] **2.2** 🟡 **Dockerfile УЖЕ на `migrate deploy`** (CMD стр.46 — `prisma migrate deploy`; агент прочитал устаревший комментарий стр.38-43, говорящий про db push). Осталось: (а) почистить устаревший комментарий; (б) проверить, что история миграций консистентна с прод-БД (прод создавался эпохой db push — возможен дрейф/нужен baseline). Done: комментарий верный, migrate deploy проходит на прод без ошибок дрейфа.
- [x] **2.3** ✅ **AI-бюджет durable** (commit `0375fa1`). Дневной AI-лимит (200/user, единственный гард стоимости Claude) был in-memory token-bucket → терялся при рестарте + не шарился между инстансами. Теперь Postgres: `AiUsageDaily(userId, day, count, @@id)` + атомарный upsert-increment per (userId, UTC-день), 429 при превышении, fail-open при сбое БД. Per-minute лимитеры роутов остались in-memory (сброс ок для анти-бёрста). Идемпотентная миграция `20260601150000_ai_usage_daily`. tsc чист, сюита 1821. *(Redis не понадобился — Postgres достаточно.)*
- [x] **2.4** ✅ **Лёгкий алертинг в Telegram** (commit `b19994c`, по выбору Berik — Sentry отложен). `lib/alert.ts alertError()` — всегда console.error + (если заданы `TELEGRAM_BOT_TOKEN`+`ALERT_TELEGRAM_CHAT_ID`) прямой POST в Bot API (независим от telegraf, работает на старте/шатдауне), троттлинг 5мин, 8с таймаут, не бросает. Подключён в process-backstop (1.5): unhandledRejection + uncaughtException. Per-task warn'ы шедулера НЕ алертим (спам). tsc чист, сюита 1827, без миграции. **⚠️ Berik: задать `ALERT_TELEGRAM_CHAT_ID` (свой chat id, напр. через @userinfobot) в Railway env — иначе алерт = только лог.**

---

## ФАЗА 3 — Стоимость и корректность

- [s] **3.1** ⏭️ **ПРОПУЩЕНО (Berik 2026-06-01).** Разведка: суммы — целые тенге (мобайл форматирует `maximumFractionDigits:0`), а Float64 хранит целые точно → реального дрейфа нет. Decimal-миграция трогает 21 файл + ломает JSON-сериализацию (Decimal→строка) для живого мобайла (`finance.tsx` ждёт `number`). Низкая ценность + высокий риск → не делаем. (Если появится дробная валюта/копейки — вернуться: Decimal + boundary `.toNumber()` + мобайл.)
- [ ] **3.2** 🟡 **ОТЛОЖЕНО до роста памяти.** Векторный поиск без индекса (нет ivfflat/hnsw) → seq-scan. Сейчас памяти мало → не проблема. Плюс deploy-риск: если pgvector на Railway не поддержит hnsw → миграция упадёт. Сделать когда объём памяти реально вырастет (и проверить версию pgvector). Не делаем сейчас.
- [ ] **3.3** 🟠 **Кризис-детектор только RU/KK**, англоязычный путь fail-open + завязан на opt-out терапевт-режима (`safety-classifier.ts:89,117`). Для глобального запуска — дыра. → EN-паттерны; Tier-2 кризиса независим от opt-out; fail-safe (показать ресурсы), не fail-open. Done: EN-кризис ловится.
- [ ] **3.4** 🟡 (после 2.3) Глобальный токен-бюджет на юзера (сейчас лимит по числу запросов, не токенов).

---

## ФАЗА 4 — App Store / Compliance (В КОНЦЕ)

- [ ] **4.1** 🔴 **Privacy policy + раскрытие суб-процессоров** (Anthropic/Voyage/Groq) + Privacy Nutrition Labels; **согласие в БД** (`consentedAt` + версия терминов); серверный гейт на принятие toxic-стиля. Done: согласие пишется, политика опубликована.
- [ ] **4.2** 🔴 **Apple Privacy Manifest `PrivacyInfo.xcprivacy`** не настроен → App Store Connect отклонит загрузку. → через `app.json`/config-plugin. Done: манифест генерится.
- [ ] **4.3** 🔴 **Age gate** (16+ EU / parental) для эмоц. компаньона с toxic-персоной (линия Replika). toxic — за принятием возраста+предупреждения. Done: гейт на регистрации.
- [ ] **4.4** 🟠 **`/subscription` → 404** (нет роута, `apps/mobile/stores/subscription-store.ts:60`) — экран подписки падает в release. → добавить роут или убрать вызов. Done: экран не падает.
- [ ] **4.5** 🟠 **EAS submit — заглушки** (`APPLE_ID_HERE`, нет `google-service-account.json`, нет записи в App Store Connect). *Операционное — Berik.*
- [ ] **4.6** 🟡 GDPR полный экспорт данных (сейчас CSV только finance/habits/tasks; нет «скачать всё»).

---

## ✅ Что НЕ трогаем (уже хорошо, подтверждено аудитом)
Мобайл настоящий и подключён к v2-мозгу · JWT/refresh-гигиена сильная · IDOR закрыт · zod-валидация · crisis-safety реально вшита (эмпатия>стиль) · account deletion готов · логи без PII + шифрование OAuth · прежние P0 (импорт/экспорт/gcal/streak/cycle/timezone) в основном закрыты.

## Продуктовый разрыв (не баг, будущая работа)
Робот-аватар + комната (см. memory `robot_avatar_replaces_pet`) в приложении ЕЩЁ нет — там старый тамагочи. Делаем при сборке мобайла, отдельно от этого плана.
