/**
 * #5 — ОДИН источник правды про умения/ограничения JARVIS.
 *
 * Проблема (из разбора): в промте рукописно «подбираю и бронирую
 * авиабилеты», а через 5 сообщений «не могу покупать билеты». Список
 * умений расходился с реальностью с каждым релизом.
 *
 * Теперь capability/constraint-текст живёт ЗДЕСЬ, рядом с честными
 * ограничениями, и `DOCUMENTED_TOOLS` сверяется тестом против
 * реального LOCAL_TOOLS-реестра: добавил инструмент, не описав —
 * тест падает. Расхождение ловится в CI, а не у юзера.
 *
 * Модуль без зависимостей (prompt не должен тащить prisma).
 */

/** Каждый инструмент LOCAL_TOOLS должен быть здесь (drift-guard). */
export const DOCUMENTED_TOOLS: ReadonlySet<string> = new Set([
  'get_tasks',
  'get_calendar',
  'get_budget',
  'create_task',
  'create_event',
  'complete_habit',
  'get_email_triage',
  'recall_person',
  'get_weekly_plan',
  'get_trip',
  'get_weather',
  'get_goal_progress',
]);

/** Что JARVIS реально делает (живым языком, для блока ДЕЙСТВИЯ). */
export const CAPABILITY_TEXT =
  'Умеешь (реально, через инструменты): задачи (создать/отметить/' +
  'посмотреть), события календаря, привычки, бюджет и финансы ' +
  '(запись расхода/дохода — с подтверждением), цели года и план на ' +
  'неделю, прогресс по целям, поездки (подбор + предзаполненная ' +
  'ссылка + чеклист + напоминания), погода, триаж непрочитанной ' +
  'почты, память о людях/фактах. Простые и обратимые (задача, ' +
  'привычка, событие) — делай сразу, не переспрашивай «создать?». ' +
  'Денежные/необратимые (расход/доход, отправить сообщение) — ' +
  'сначала подтверждение и дождись «да».';

/** Честные ОГРАНИЧЕНИЯ — ровно то, чего НЕ делаешь (не ври). */
export const CONSTRAINTS_TEXT =
  'Чего ты НЕ делаешь (говори честно, не обещай): билеты/отели — ' +
  'подбираешь и даёшь рабочую ссылку, но НЕ покупаешь и НЕ ' +
  'оплачиваешь (оплата у юзера, авто-списания нет); картинки/фото ' +
  'НЕ генерируешь; журнал звонков, переписку в мессенджерах и ' +
  'запись разговоров НЕ видишь (закрыто на iOS/по закону) — на ' +
  '«когда я звонил маме» честно скажи, что не видишь, и предложи ' +
  'поставить напоминание/задачу; запись в Google Calendar/Gmail — ' +
  'только после подключения интеграции. Лучше честное «не умею ' +
  'X, но могу Y», чем выдумка.';
