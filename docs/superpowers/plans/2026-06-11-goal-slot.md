# Goal-slot (цель ↔ свободный слот) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** `goal_behind`-нудж получает слот-якорь из календаря («В четверг в 18:00 свободно ~60 мин — поставить занятие? Скажи "да"») + доставленный нудж пишется в историю чата, чтобы «да» работало.

**Architecture:** `goal-slot.ts` (`ruDayName`+`findUpcomingSlot`+`formatGoalSlotTail`, read-only) → Part 1 врезка в `proactive-insights.ts` goal_behind (top-1 цель) → Part 2 врезка в `insight-store.ts` deliverTopInsight (ChatMessage-persist). Один флаг `FEATURE_V2_GOAL_SLOT`, off=байт-идентично.

**Спека:** `docs/superpowers/specs/2026-06-11-goal-slot-design.md`.

**Tech:** Fastify+Prisma6+Postgres, ESM `.js`, TS strict no `any`, vitest, zero `vi.mock`. MAIN worktree `/Users/berikkurmangoliev/Desktop/LifeOS` (branch=main). Коммит-на-шаг trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. push/deploy/флаг — по слову Berik.

---

### Task 1: Flag `isV2GoalSlotEnabled`
**Files:** `src/lib/feature-flags.ts` (+`.test.ts`)
- [ ] **S1** тест (рядом с `isV2OpenLoopsEnabled`):
```ts
import { isV2GoalSlotEnabled } from './feature-flags.js';
describe('isV2GoalSlotEnabled', () => {
  const KEY = 'FEATURE_V2_GOAL_SLOT'; const prev = process.env[KEY];
  afterEach(() => { if (prev === undefined) delete process.env[KEY]; else process.env[KEY] = prev; });
  it('all→true', () => { process.env[KEY]='all'; expect(isV2GoalSlotEnabled('u')).toBe(true); });
  it('unset/none/empty→false', () => { delete process.env[KEY]; expect(isV2GoalSlotEnabled('u')).toBe(false); process.env[KEY]='none'; expect(isV2GoalSlotEnabled('u')).toBe(false); process.env[KEY]=''; expect(isV2GoalSlotEnabled('u')).toBe(false); });
  it('csv', () => { process.env[KEY]='user-abc'; expect(isV2GoalSlotEnabled('abc')).toBe(true); expect(isV2GoalSlotEnabled('z')).toBe(false); });
});
```
- [ ] **S2** FAIL: `cd /Users/berikkurmangoliev/Desktop/LifeOS/packages/server && npx vitest run --project unit src/lib/feature-flags.test.ts`
- [ ] **S3** impl (рядом с `isV2OpenLoopsEnabled`):
```ts
/**
 * Цель↔слот: goal_behind-нудж получает слот-якорь («в чт 18:00 свободно — поставить?»)
 * + доставленный нудж пишется в ChatMessage (ответ «да» работает). OFF → сообщение
 * прежнее + ничего не пишется → байт-идентично.
 */
export function isV2GoalSlotEnabled(userId: string): boolean {
  return isEnabledForUser(process.env.FEATURE_V2_GOAL_SLOT, userId);
}
```
- [ ] **S4** PASS+tsc: `… && npx vitest run --project unit src/lib/feature-flags.test.ts && npx tsc --noEmit`
- [ ] **S5** commit:
```bash
cd /Users/berikkurmangoliev/Desktop/LifeOS && git add packages/server/src/lib/feature-flags.ts packages/server/src/lib/feature-flags.test.ts && git commit -m "$(cat <<'EOF'
feat(crossdomain): isV2GoalSlotEnabled flag (off=identical)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: `goal-slot.ts` — ruDayName + findUpcomingSlot + formatGoalSlotTail
**Files:** Create `src/services/goal-slot.ts` + `src/services/goal-slot.test.ts`
- [ ] **S1** тест `goal-slot.test.ts` (чистые функции; findUpcomingSlot — в it):
```ts
import { describe, it, expect } from 'vitest';
import { ruDayName, formatGoalSlotTail } from './goal-slot.js';

describe('ruDayName', () => {
  it('известные даты', () => {
    expect(ruDayName('2026-06-11')).toBe('четверг');  // 2026-06-11 = Thu
    expect(ruDayName('2026-06-12')).toBe('пятницу');
    expect(ruDayName('2026-06-14')).toBe('воскресенье');
  });
});
describe('formatGoalSlotTail', () => {
  it('день+дата+время+CTA', () => {
    const s = formatGoalSlotTail({ date: '2026-06-11', from: '18:00', to: '20:00', durationMinutes: 120 });
    expect(s).toContain('четверг'); expect(s).toContain('11.06');
    expect(s).toContain('18:00'); expect(s).toContain('Скажи «да»');
  });
  it('durationMinutes > 120 капится до ~120', () => {
    const s = formatGoalSlotTail({ date: '2026-06-12', from: '09:00', to: '20:00', durationMinutes: 660 });
    expect(s).toContain('~120 мин');
  });
});
```
(Перед написанием ПРОВЕРЬ день недели 2026-06-11 командой `node -e "console.log(new Date('2026-06-11T00:00:00Z').getUTCDay())"` — 4=четверг; если иное, поправь ожидания на фактические.)
- [ ] **S2** FAIL: `… && npx vitest run --project unit src/services/goal-slot.test.ts`
- [ ] **S3** impl `goal-slot.ts` (полный код из спеки §«Юнит A»; сверь экспорты `findFreeSlots`/`FreeSlot` из `'../tools/_slots.js'`, `getUserTimezone` из `'../lib/user-context.js'`, `localDateStr` из `'../lib/tz.js'` — если `localDateStr` не экспортирован, экспортируй его в tz.ts ИЛИ используй существующий экспорт, дающий civil 'YYYY-MM-DD' юзера):
```ts
import { prisma } from '../lib/prisma.js';
import { findFreeSlots, type FreeSlot } from '../tools/_slots.js';
import { getUserTimezone } from '../lib/user-context.js';
import { localDateStr } from '../lib/tz.js';

const RU_DAYS = ['воскресенье', 'понедельник', 'вторник', 'среду', 'четверг', 'пятницу', 'субботу'];

/** Имя дня (вин. падеж «в …») из civil 'YYYY-MM-DD'. TZ-безопасно (UTC-аксессоры). */
export function ruDayName(civilDate: string): string {
  return RU_DAYS[new Date(civilDate + 'T00:00:00Z').getUTCDay()];
}

/**
 * Ближайшее свободное окно ≥minDuration в [завтра..+horizonДн] по календарю юзера.
 * READ-ONLY. Завтра (не сегодня): findFreeSlots не учитывает «уже прошло сегодня».
 * Civil-даты юзера (convA, как get-free-slots). null = окна нет / ошибка (best-effort).
 */
export async function findUpcomingSlot(
  userId: string,
  now: Date = new Date(),
  minDurationMinutes = 45,
  horizonDays = 7,
): Promise<FreeSlot | null> {
  try {
    const tz = await getUserTimezone(userId);
    const todayCivil = localDateStr(tz, now);
    const dayMs = 86_400_000;
    const fromDate = new Date(new Date(todayCivil + 'T00:00:00Z').getTime() + dayMs);
    const toDate = new Date(fromDate.getTime() + (horizonDays - 1) * dayMs);
    const events = await prisma.calendarEvent.findMany({
      where: { userId, date: { gte: fromDate, lte: toDate } },
      select: { date: true, startTime: true, endTime: true },
      orderBy: [{ date: 'asc' }, { startTime: 'asc' }],
      take: 200,
    });
    const slots = findFreeSlots(events, fromDate, toDate, minDurationMinutes);
    return slots[0] ?? null;
  } catch {
    return null;
  }
}

/** Чистый хвост-предложение к goal_behind-сообщению. */
export function formatGoalSlotTail(slot: FreeSlot): string {
  const mins = Math.min(slot.durationMinutes, 120);
  return ` В ${ruDayName(slot.date)} (${slot.date.slice(8, 10)}.${slot.date.slice(5, 7)}) в ${slot.from} свободно ~${mins} мин — поставить занятие по цели? Скажи «да» — закину в план.`;
}
```
(`localDateStr` сигнатуру сверь по tz.ts — там используется внутри `localDayStartUTC` как `localDateStr(zone, at)`; если приватный — добавь `export` к нему, это безопасно.)
- [ ] **S4** PASS+tsc: `… && npx vitest run --project unit src/services/goal-slot.test.ts && npx tsc --noEmit`
- [ ] **S5** commit:
```bash
cd /Users/berikkurmangoliev/Desktop/LifeOS && git add packages/server/src/services/goal-slot.ts packages/server/src/services/goal-slot.test.ts packages/server/src/lib/tz.ts && git commit -m "$(cat <<'EOF'
feat(crossdomain): goal-slot helpers — findUpcomingSlot + formatGoalSlotTail (read-only)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```
(tz.ts в add только если правил export.)

---

### Task 3: Part 1 — слот-хвост в `proactive-insights.ts`
**Files:** Modify `src/services/proactive-insights.ts`; Create `src/services/goal-slot-wiring.test.ts`
- [ ] **S1** создать `goal-slot-wiring.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';

describe('goal-slot — Part 1 proactive-insights', () => {
  const src = readFileSync(join(process.cwd(), 'src/services/proactive-insights.ts'), 'utf8');
  it('флаг-гейт + findUpcomingSlot + formatGoalSlotTail в goal_behind', () => {
    expect(src).toMatch(/isV2GoalSlotEnabled\(/);
    expect(src).toMatch(/findUpcomingSlot\(/);
    expect(src).toMatch(/formatGoalSlotTail\(/);
  });
  it('прежний литерал сообщения сохранён (off=identical)', () => {
    expect(src).toMatch(/Отстаём — давай наверстаем\?/);
  });
});
```
- [ ] **S2** FAIL: `… && npx vitest run --project unit src/services/goal-slot-wiring.test.ts`
- [ ] **S3** impl: ОТКРОЙ `proactive-insights.ts` блок goal_behind (≈:472-489, `for (const g of behind)`). Сверь: имя переменной userId в области видимости билдера (функция async? как называется параметр юзера?), `now` в области. Замени петлю так, чтобы:
  - для индекса 0 (top-gap цель) при `isV2GoalSlotEnabled(userId)`: `const slot = await findUpcomingSlot(userId, now); if (slot) message += formatGoalSlotTail(slot);`
  - message строится из ТОЧНО ПРЕЖНЕГО литерала (`Ты ставил цель «${g.goalText}» (${g.area}): выполнено ~${g.progressPct}%, а год прошёл на ${expectedPct}%. Отстаём — давай наверстаем?`) + опц. хвост;
  - объект insights.push — все остальные поля БЕЗ изменений.
  Импорты вверху: `isV2GoalSlotEnabled` из `'../lib/feature-flags.js'`; `findUpcomingSlot, formatGoalSlotTail` из `'./goal-slot.js'`. Если петля `for (const g of behind)` — перепиши на `for (const [i, g] of behind.entries())` или эквивалент с индексом. **off** → message ровно прежний → байт-идентично.
- [ ] **S4** целевой+ПОЛНЫЙ unit+tsc: `… && npx vitest run --project unit src/services/goal-slot-wiring.test.ts && npm test && npx tsc --noEmit`. Бриттл фикс-окно из-за +строк → сообщи, не маскируй.
- [ ] **S5** commit:
```bash
cd /Users/berikkurmangoliev/Desktop/LifeOS && git add packages/server/src/services/proactive-insights.ts packages/server/src/services/goal-slot-wiring.test.ts && git commit -m "$(cat <<'EOF'
feat(crossdomain): goal_behind nudge gets calendar slot anchor behind flag (Part 1)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Part 2 — нудж в историю чата (`insight-store.ts`)
**Files:** Modify `src/services/insight-store.ts`; дополнить `goal-slot-wiring.test.ts`
- [ ] **S1** добавить в `goal-slot-wiring.test.ts`:
```ts
describe('goal-slot — Part 2 reply-context', () => {
  const store = readFileSync(join(process.cwd(), 'src/services/insight-store.ts'), 'utf8');
  it('за флагом пишет доставленный нудж в ChatMessage как assistant', () => {
    expect(store).toMatch(/isV2GoalSlotEnabled\(/);
    expect(store).toMatch(/chatMessage[\s\S]*?\.create\(/);
    expect(store).toMatch(/role:\s*'assistant'/);
  });
});
```
- [ ] **S2** FAIL: `… && npx vitest run --project unit src/services/goal-slot-wiring.test.ts`
- [ ] **S3** impl: в `deliverTopInsight` (`insight-store.ts:248-263`) ПОСЛЕ успешной доставки и `deliveredAt`-апдейта добавить:
```ts
  // Reply-context: доставленный нудж — в историю чата, чтобы ответ «да»
  // пришёл агенту С контекстом предложения (иначе «да» — в пустоту).
  if (isV2GoalSlotEnabled(userId)) {
    await prisma.chatMessage
      .create({ data: { userId, role: 'assistant', content: row.message } })
      .catch(() => {});
  }
```
Импорт `isV2GoalSlotEnabled` из `'../lib/feature-flags.js'` (сверь существующий импорт-блок). `prisma` уже импортирован в файле (сверь). **off** → ничего не пишется → байт-идентично.
- [ ] **S4** целевой+ПОЛНЫЙ unit+tsc: `… && npx vitest run --project unit src/services/goal-slot-wiring.test.ts && npm test && npx tsc --noEmit`.
- [ ] **S5** commit:
```bash
cd /Users/berikkurmangoliev/Desktop/LifeOS && git add packages/server/src/services/insight-store.ts packages/server/src/services/goal-slot-wiring.test.ts && git commit -m "$(cat <<'EOF'
feat(crossdomain): delivered nudge persists to chat history behind flag (Part 2 reply-context)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Integration tests
**Files:** Create `src/services/goal-slot.it.test.ts`
> Harness — образец `open-loops.it.test.ts` (own PrismaClient, mkUser tz Asia/Almaty).
- [ ] **S1** `npm run test:db:up`, затем:
```ts
import { describe, it, expect, afterAll } from 'vitest';
import { PrismaClient } from '@prisma/client';
import { findUpcomingSlot } from './goal-slot.js';

const prisma = new PrismaClient();
afterAll(async () => { await prisma.$disconnect(); });
const TZ = 'Asia/Almaty';
async function mkUser(tag: string): Promise<string> {
  const u = await prisma.user.create({ data: { email: `it-gslot-${tag}@it.local`, name: 'IT', passwordHash: 'x', timezone: TZ } });
  return u.id;
}
// civil завтра/послезавтра в TZ юзера (как считает findUpcomingSlot)
function civilPlus(days: number): string {
  const fmt = new Intl.DateTimeFormat('en-CA', { timeZone: TZ, year: 'numeric', month: '2-digit', day: '2-digit' });
  return fmt.format(new Date(Date.now() + days * 86_400_000));
}

describe('findUpcomingSlot', () => {
  it('пустой календарь → слот завтра 09:00 (полное окно)', async () => {
    const uid = await mkUser(`e-${Date.now()}`);
    const slot = await findUpcomingSlot(uid);
    expect(slot).not.toBeNull();
    expect(slot!.date).toBe(civilPlus(1));
    expect(slot!.from).toBe('09:00');
    expect(slot!.durationMinutes).toBe(660);
  });
  it('завтра занято целиком → слот послезавтра', async () => {
    const uid = await mkUser(`b-${Date.now()}`);
    await prisma.calendarEvent.create({ data: {
      userId: uid, title: 'весь день', date: new Date(civilPlus(1) + 'T00:00:00Z'),
      startTime: '09:00', endTime: '20:00',
    } });
    const slot = await findUpcomingSlot(uid);
    expect(slot).not.toBeNull();
    expect(slot!.date).toBe(civilPlus(2));
  });
  it('cross-user: чужие события не влияют', async () => {
    const u1 = await mkUser(`x1-${Date.now()}`);
    const u2 = await mkUser(`x2-${Date.now()}`);
    await prisma.calendarEvent.create({ data: {
      userId: u2, title: 'чужое', date: new Date(civilPlus(1) + 'T00:00:00Z'),
      startTime: '09:00', endTime: '20:00',
    } });
    const slot = await findUpcomingSlot(u1);
    expect(slot!.date).toBe(civilPlus(1)); // у u1 завтра свободно
  });
});
```
Замечания: сверь обязательные поля `CalendarEvent.create` по schema.prisma (если есть обязательные кроме userId/title/date — добавь валидные). civilPlus аппроксимирует «+N дней» — на границе локальной полуночи может дать ±1; если флаки на полуночи — вычисляй через тот же `localDateStr`-хелпер из tz.ts. Красное → Phase-1 debug.
- [ ] **S2** run: `cd /Users/berikkurmangoliev/Desktop/LifeOS/packages/server && npx vitest run --project integration src/services/goal-slot.it.test.ts` — 3 PASS.
- [ ] **S3** commit:
```bash
cd /Users/berikkurmangoliev/Desktop/LifeOS && git add packages/server/src/services/goal-slot.it.test.ts && git commit -m "$(cat <<'EOF'
test(crossdomain): goal-slot it — nearest slot, busy-day skip, cross-user

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: Full verify + independent review
- [ ] **S1** `cd /Users/berikkurmangoliev/Desktop/LifeOS/packages/server && npm test && npx tsc --noEmit`.
- [ ] **S2** it: goal-slot + open-loops + mem-graph (leak-чек).
- [ ] **S3 OFF-проверка** — diff: Part1 message-литерал прежний при off; Part2 не пишет при off; Part1 read-only.
- [ ] **S4 независимое ревью** (code-reviewer opus). Фокус: off=байт-идентично оба; Part1 read-only + слот реальный (антифаб) + TZ civil-даты; Part2 пишет ТОЛЬКО ChatMessage (role assistant, crisis-дефолт false), best-effort, не ломает доставку/crisis-изоляцию; «да»-поток через getRecentHistory работает; нет `any`/ESM/`vi.mock`.
- [ ] **S5** fix findings + commit.
- [ ] **S6 СТОП** — доложить Berik. Не пушить. Предложить push+deploy+`FEATURE_V2_GOAL_SLOT=all`.

---

## Self-Review
- **Покрытие спеки:** флаг (T1), хелперы (T2), Part1 (T3), Part2 (T4), it (T5), verify (T6).
- **Заглушки:** код полный; «сверь scope/экспорт/обязательные поля» — явная сверка с реальностью.
- **Типы:** `ruDayName(civil)→string`; `findUpcomingSlot(userId,now?,minDur?,horizon?)→Promise<FreeSlot|null>`; `formatGoalSlotTail(FreeSlot)→string`.
- **off=identical:** Part1 литерал прежний (структурный тест), Part2 гейт.

## Execution Handoff
Subagent-Driven. push/deploy/флаг — по слову Berik.
