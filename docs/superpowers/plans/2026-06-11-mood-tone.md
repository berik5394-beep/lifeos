# Mood-tone (тон-по-настроению) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`.

**Goal:** Mood-сдвиг вниз → (1) инструкция «мягче, не наваливай» в промпт (перекрывает стиль); (2) «давящие» нуджи (`PRESSURE_SOURCES`) глушатся; поддержка/время-критичное/деньги-защита остаются.

**Architecture:** Сигнал — существующий `detectMoodShift`. Part 1 — поле `moodToneDirective` в enrichment-data (паттерн openLoops) + push в форматтере. Part 2 — `PRESSURE_SOURCES` + чистый `suppressPressureNudges` + врезка в `filterCandidates`. Флаг `FEATURE_V2_MOOD_TONE`, off=байт-идентично.

**Спека:** `docs/superpowers/specs/2026-06-11-mood-tone-design.md`.

**Tech:** Fastify+Prisma6+Postgres, ESM `.js`, TS strict no `any`, vitest, zero `vi.mock`. MAIN worktree `/Users/berikkurmangoliev/Desktop/LifeOS` (branch=main). Trailer `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`. push/deploy/флаг — по слову Berik.

---

### Task 1: Flag `isV2MoodToneEnabled`
**Files:** `src/lib/feature-flags.ts` (+`.test.ts`)
- [ ] **S1** тест (рядом с `isV2GoalSlotEnabled`):
```ts
import { isV2MoodToneEnabled } from './feature-flags.js';
describe('isV2MoodToneEnabled', () => {
  const KEY = 'FEATURE_V2_MOOD_TONE'; const prev = process.env[KEY];
  afterEach(() => { if (prev === undefined) delete process.env[KEY]; else process.env[KEY] = prev; });
  it('all→true', () => { process.env[KEY]='all'; expect(isV2MoodToneEnabled('u')).toBe(true); });
  it('unset/none/empty→false', () => { delete process.env[KEY]; expect(isV2MoodToneEnabled('u')).toBe(false); process.env[KEY]='none'; expect(isV2MoodToneEnabled('u')).toBe(false); process.env[KEY]=''; expect(isV2MoodToneEnabled('u')).toBe(false); });
  it('csv', () => { process.env[KEY]='user-abc'; expect(isV2MoodToneEnabled('abc')).toBe(true); expect(isV2MoodToneEnabled('z')).toBe(false); });
});
```
- [ ] **S2** FAIL: `cd /Users/berikkurmangoliev/Desktop/LifeOS/packages/server && npx vitest run --project unit src/lib/feature-flags.test.ts`
- [ ] **S3** impl:
```ts
/**
 * Тон-по-настроению: mood-сдвиг вниз → (1) инструкция «мягче» в промпт (перекрывает
 * стиль), (2) «давящие» нуджи глушатся (PRESSURE_SOURCES). OFF → только факт-строка
 * настроения + все нуджи как раньше → байт-идентично.
 */
export function isV2MoodToneEnabled(userId: string): boolean {
  return isEnabledForUser(process.env.FEATURE_V2_MOOD_TONE, userId);
}
```
- [ ] **S4** PASS+tsc. **S5** commit:
```bash
cd /Users/berikkurmangoliev/Desktop/LifeOS && git add packages/server/src/lib/feature-flags.ts packages/server/src/lib/feature-flags.test.ts && git commit -m "$(cat <<'EOF'
feat(crossdomain): isV2MoodToneEnabled flag (off=identical)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: Part 1 — тон-директива в enrichment
**Files:** Modify `src/services/v2-enrichment.ts`; Create `src/services/mood-tone-wiring.test.ts`
- [ ] **S1** создать `mood-tone-wiring.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { join } from 'node:path';

describe('mood-tone — Part 1 enrichment directive', () => {
  const enr = readFileSync(join(process.cwd(), 'src/services/v2-enrichment.ts'), 'utf8');
  it('флаг-гейт + директива при direction down', () => {
    expect(enr).toMatch(/isV2MoodToneEnabled\(/);
    expect(enr).toMatch(/moodToneDirective/);
    expect(enr).toMatch(/будь мягче/);
    expect(enr).toMatch(/перекрывает выбранный стиль/);
  });
});
```
- [ ] **S2** FAIL: `… && npx vitest run --project unit src/services/mood-tone-wiring.test.ts`
- [ ] **S3** impl (паттерн openLoops — поле в data, т.к. `buildV2EnrichmentBlock(data)` чистый):
  3a. Импорт `isV2MoodToneEnabled` в существующий блок из `'../lib/feature-flags.js'`.
  3b. В `V2EnrichmentData`: `moodToneDirective: string | null;` (рядом с `moodShift`).
  3c. В `fetchV2EnrichmentData` — moodShift уже фетчится в Promise.all и доступен после destructure. ПОСЛЕ destructure (перед сборкой возвращаемого объекта) вычислить:
```ts
    const moodToneDirective =
      isV2MoodToneEnabled(userId) && moodShift?.shifted && moodShift.direction === 'down'
        ? 'юзер в эмоциональном спаде последние дни — будь мягче: больше поддержки, ' +
          'меньше требований и критики, не наваливай задачи и цели. ' +
          'Это перекрывает выбранный стиль (даже strict/toxic).'
        : null;
```
  и добавить `moodToneDirective` в возвращаемый объект. (СВЕРЬ имя переменной moodShift в destructure.)
  3d. В `buildV2EnrichmentBlock` СРАЗУ после факт-блока moodShift (`:129-134`): `if (data.moodToneDirective) lines.push(data.moodToneDirective);`
  **off** → null → не пушится → байт-идентично.
- [ ] **S4** целевой + ПОЛНЫЙ unit + tsc: `… && npx vitest run --project unit src/services/mood-tone-wiring.test.ts && npm test && npx tsc --noEmit`. Бриттл фикс-окно → сообщи.
- [ ] **S5** commit:
```bash
cd /Users/berikkurmangoliev/Desktop/LifeOS && git add packages/server/src/services/v2-enrichment.ts packages/server/src/services/mood-tone-wiring.test.ts && git commit -m "$(cat <<'EOF'
feat(crossdomain): mood-down tone directive in enrichment behind flag (Part 1)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: Part 2 — PRESSURE_SOURCES + suppressPressureNudges + врезка
**Files:** Modify `src/services/v2-proactivity-engine.ts`; дополнить `mood-tone-wiring.test.ts`
- [ ] **S1** добавить в `mood-tone-wiring.test.ts` (импорт engine-функций — unit, не structural):
```ts
import { suppressPressureNudges, PRESSURE_SOURCES, type NudgeCandidate } from './v2-proactivity-engine.js';

describe('mood-tone — Part 2 suppressPressureNudges (pure)', () => {
  const mk = (source: NudgeCandidate['source']): NudgeCandidate =>
    ({ source, significance: 0.7, payload: {}, toneHint: 'gentle' }) as NudgeCandidate;
  const down = { shifted: true, direction: 'down' as const };
  it('down → pressure выкинут, поддержка/время-критичное остались', () => {
    const out = suppressPressureNudges([mk('goal_no_progress'), mk('mood_shift'), mk('person_meeting')], down);
    expect(out.map((c) => c.source)).toEqual(['mood_shift', 'person_meeting']);
  });
  it('up / null / not-shifted → нетронуто', () => {
    const cands = [mk('goal_no_progress')];
    expect(suppressPressureNudges(cands, { shifted: true, direction: 'up' })).toEqual(cands);
    expect(suppressPressureNudges(cands, null)).toEqual(cands);
    expect(suppressPressureNudges(cands, { shifted: false })).toEqual(cands);
  });
  it('PRESSURE_SOURCES не глушит поддержку (анти-регресс)', () => {
    for (const keep of ['mood_shift', 'person_meeting', 'obligation_due', 'runway_low', 'birthday_upcoming', 'commitment_due'] as const) {
      expect(PRESSURE_SOURCES.has(keep)).toBe(false);
    }
    expect(PRESSURE_SOURCES.size).toBe(10);
  });
});
describe('mood-tone — Part 2 wiring', () => {
  const eng = readFileSync(join(process.cwd(), 'src/services/v2-proactivity-engine.ts'), 'utf8');
  it('filterCandidates зовёт suppress за флагом', () => {
    expect(eng).toMatch(/isV2MoodToneEnabled\(/);
    expect(eng).toMatch(/suppressPressureNudges\(/);
  });
});
```
(NudgeCandidate-каст `as NudgeCandidate` допустим в тесте, если у типа есть доп. обязательные поля — сверь и заполни честно при необходимости.)
- [ ] **S2** FAIL: `… && npx vitest run --project unit src/services/mood-tone-wiring.test.ts`
- [ ] **S3** impl в `v2-proactivity-engine.ts`:
  3a. Экспорт-константа + чистая функция (код из спеки §«Юнит B» дословно: `PRESSURE_SOURCES` ровно 10 членов; `suppressPressureNudges(candidates, shift)`). Разместить рядом с гейтами/хелперами.
  3b. Импорт `isV2MoodToneEnabled` в существующий feature-flags-импорт.
  3c. Врезка в `filterCandidates` (:1284-1309) ПОСЛЕ `if (!(await gate2_RateLimit(userId, now))) return [];` и ПЕРЕД `let sigThreshold = 0.6;`:
```ts
    if (isV2MoodToneEnabled(userId)) {
      const shift = await getEmotionalMemory()
        .detectMoodShift(userId)
        .catch(() => null);
      candidates = suppressPressureNudges(candidates, shift);
      if (candidates.length === 0) return [];
    }
```
  (`candidates` — параметр функции; переприсваивание параметра ок, или локальная переменная. `getEmotionalMemory` уже импортирован :335.) **off** → ветка не выполняется → байт-идентично.
- [ ] **S4** целевой + ПОЛНЫЙ unit + tsc: `… && npx vitest run --project unit src/services/mood-tone-wiring.test.ts && npm test && npx tsc --noEmit`.
- [ ] **S5** commit:
```bash
cd /Users/berikkurmangoliev/Desktop/LifeOS && git add packages/server/src/services/v2-proactivity-engine.ts packages/server/src/services/mood-tone-wiring.test.ts && git commit -m "$(cat <<'EOF'
feat(crossdomain): suppress pressure nudges on mood-down behind flag (Part 2)

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: Integration test — сигнал-путь detectMoodShift
**Files:** Create `src/services/mood-tone.it.test.ts`
> Гейты filterCandidates сквозняком НЕ гоняем (gate1_DND зависит от часа = флаки). Логика глушения покрыта юнитом; здесь — честность СИГНАЛА на сидах.
- [ ] **S1** `npm run test:db:up`, затем:
```ts
import { describe, it, expect, afterAll } from 'vitest';
import { PrismaClient } from '@prisma/client';
import { getEmotionalMemory } from './emotional-memory.singleton.js';

const prisma = new PrismaClient();
afterAll(async () => { await prisma.$disconnect(); });
async function mkUser(tag: string): Promise<string> {
  const u = await prisma.user.create({ data: { email: `it-mood-${tag}@it.local`, name: 'IT', passwordHash: 'x', timezone: 'Asia/Almaty' } });
  return u.id;
}
const daysAgo = (d: number) => new Date(Date.now() - d * 86_400_000);

describe('mood-tone — сигнал detectMoodShift', () => {
  it('baseline высокий + recent низкий → shifted down', async () => {
    const uid = await mkUser(`d-${Date.now()}`);
    // baseline: 6 строк valence +0.5 в днях 5..15 назад
    for (let i = 0; i < 6; i++) {
      await prisma.moodSnapshot.create({ data: { userId: uid, source: 'message', valence: 0.5, arousal: 0.5, emotion: 'happy', recordedAt: daysAgo(5 + i * 2) } });
    }
    // recent: 3 строки valence -0.5 в последние 2 дня
    for (let i = 0; i < 3; i++) {
      await prisma.moodSnapshot.create({ data: { userId: uid, source: 'message', valence: -0.5, arousal: 0.5, emotion: 'sad', recordedAt: daysAgo(i * 0.5) } });
    }
    const shift = await getEmotionalMemory().detectMoodShift(uid);
    expect(shift?.shifted).toBe(true);
    expect(shift?.direction).toBe('down');
  });
  it('ровное настроение → не down', async () => {
    const uid = await mkUser(`f-${Date.now()}`);
    for (let i = 0; i < 9; i++) {
      await prisma.moodSnapshot.create({ data: { userId: uid, source: 'message', valence: 0.3, arousal: 0.5, emotion: 'happy', recordedAt: daysAgo(i * 1.5) } });
    }
    const shift = await getEmotionalMemory().detectMoodShift(uid);
    expect(shift?.shifted && shift?.direction === 'down').toBeFalsy();
  });
});
```
Сверь обязательные поля `MoodSnapshot.create` по schema (entityRefs default [], excerpt опц.). Если baseline-окно detectMoodShift (4-17д) не совпадает с сидами — подгони recordedAt под фактические границы (`emotional-memory.ts:271-305`), НЕ меняй прод-логику. Если detectMoodShift у ровного юзера возвращает null (мало baseline) — это тоже «не down», тест валиден.
- [ ] **S2** run: `… && npx vitest run --project integration src/services/mood-tone.it.test.ts` — 2 PASS.
- [ ] **S3** commit:
```bash
cd /Users/berikkurmangoliev/Desktop/LifeOS && git add packages/server/src/services/mood-tone.it.test.ts && git commit -m "$(cat <<'EOF'
test(crossdomain): mood-tone it — detectMoodShift down on seeded snapshots

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Full verify + independent review
- [ ] **S1** `npm test && npx tsc --noEmit`. **S2** it: mood-tone + соседи (leak).
- [ ] **S3 OFF-проверка** — diff: Part1 null→не пушится; Part2 ветка за флагом; 0 записей.
- [ ] **S4 независимое ревью** (code-reviewer opus). Фокус: off=байт-идентично оба; read-only; кризис-путь НЕ тронут; mood_shift/поддержка НЕ в PRESSURE (анти-регресс); best-effort (catch→null→не глушим); PRESSURE-список = ровно 10 одобренных; директива не ломает существующую факт-строку.
- [ ] **S5** fix findings + commit. **S6 СТОП** — доложить Berik, предложить push+deploy+`FEATURE_V2_MOOD_TONE=all`.

---

## Self-Review
- Покрытие: флаг (T1), Part1 (T2), Part2 (T3), it-сигнал (T4), verify (T5). Все юниты спеки.
- Заглушки: код полный; «сверь destructure-имя/NudgeCandidate-поля/окна detectMoodShift» — сверка с реальностью.
- Типы: `suppressPressureNudges(NudgeCandidate[], {shifted,direction?}|null)→NudgeCandidate[]`; `PRESSURE_SOURCES: ReadonlySet<NudgeSource>` (10); `moodToneDirective: string|null`.
- off=identical: Part1 null-поле; Part2 флаг-ветка. Структурные тесты держат.

## Execution Handoff
Subagent-Driven. push/deploy/флаг — по слову Berik.
